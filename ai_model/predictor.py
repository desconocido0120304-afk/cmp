import numpy as np\nfrom sklearn.ensemble import IsolationForest\n\nclass SensorPredictor:\n    def __init__(self):\n        # Initialize the model\n        self.model = IsolationForest(n_estimators=100, contamination='auto')\n\n    def fit(self, data):\n        # Fit the model on the input data\n        self.model.fit(data)\n\n    def predict(self, new_data):\n        # Predict anomalies in new data\n        return self.model.predict(new_data)\n\n    def predict_trend(self, data):\n        # Generate trend predictions (dummy implementation)\n        trend = np.mean(data, axis=0)  # average as a simple trend\n        return trend